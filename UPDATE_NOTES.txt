Vellum 上传说明 - cloud worker sync fix

本轮实际改动：
- index.html
- public/index.html

修复内容：
- 把页面加载脚本从旧缓存版本 restore-ui-v162-20260630 改为 cloud-worker-sync-fix-20260701。
- 目的：让手机/新设备强制加载当前 app.js，而不是继续吃旧同步逻辑。

云端检查结果：
- Cloudflare Worker 根路径可访问。
- /api/v2/index?syncCode=XU8J-99LY-WKAA&lite=1 可返回目录。
- /api/v2/progress?syncCode=XU8J-99LY-WKAA 可返回阅读进度。
- CORS 正常。

GitHub 上传覆盖路径：
- 根目录：index.html, app.js, styles.css, sw.js, manifest.webmanifest, vellum-logo.png（如果存在）
- public/：index.html, app.js, styles.css, sw.js, manifest.webmanifest, vellum-logo.png（如果存在）
- cloudflare-worker/worker.js

上传后访问：
https://yc1894386-png.github.io/pocket-reading-vault/?v=cloud-worker-sync-fix-20260701

如果新设备还是卡在“正在读取云端目录”：
1. 先确认打开的是上面的新链接。
2. 手机浏览器清除该站点缓存。
3. Cloudflare Worker 代码也需要部署到 Worker 后台，GitHub 上传 worker.js 不会自动部署 Worker。
